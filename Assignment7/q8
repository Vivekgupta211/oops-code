#include <iostream>
using namespace std;

class Array {
    int a[5];
public:
    int& operator[](int i) {
        if(i>=5) {
            cout << "Out of bounds\n";
            exit(0);
        }
        return a[i];
    }
};

int main() {
    Array arr;
    arr[2] = 10;
    cout << arr[2];
}
